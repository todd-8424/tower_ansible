# file for roles variables
username: tower
password: test
domain: example
fname: test
lname: user
tusername: "{{ lookup('hashi_vault', 'secret=tower/data/tower:data token='\"{{ vault_token }}\" ' url=http://tklx00002.k24.lan:8200 validate_certs=False')|json_query('username')}}"
tpassword: "{{ lookup('cypher','secret=tower/tower/password') }}"
thost: "{{ lookup('cypher','secret=tower/tower/host') }}"
